import { AlignItemsDirective } from './align-items.directive';

describe('AlignItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignItemsDirective();
    expect(directive).toBeTruthy();
  });
});
