import { AlignCardDirective } from './align-card.directive';

describe('AlignCardDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignCardDirective();
    expect(directive).toBeTruthy();
  });
});
