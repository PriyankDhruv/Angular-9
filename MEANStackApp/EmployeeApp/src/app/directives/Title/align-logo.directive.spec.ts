import { AlignLogoDirective } from './align-logo.directive';

describe('AlignLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignLogoDirective();
    expect(directive).toBeTruthy();
  });
});
