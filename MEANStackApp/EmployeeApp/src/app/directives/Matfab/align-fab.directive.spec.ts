import { AlignFabDirective } from './align-fab.directive';

describe('AlignFabDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignFabDirective();
    expect(directive).toBeTruthy();
  });
});
