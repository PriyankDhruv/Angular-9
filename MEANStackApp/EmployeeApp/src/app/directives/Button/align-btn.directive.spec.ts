import { AlignBtnDirective } from './align-btn.directive';

describe('AlignBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignBtnDirective();
    expect(directive).toBeTruthy();
  });
});
