import { AlignListDirective } from './align-list.directive';

describe('AlignListDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignListDirective();
    expect(directive).toBeTruthy();
  });
});
